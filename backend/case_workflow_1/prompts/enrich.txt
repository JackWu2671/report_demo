你是知识图谱节点设计专家。

以下是从专家描述中提取的新知识节点，请为每个节点补充关键词，并确认或补全描述。

要求：
1. keywords: 3~6 个代表该节点的核心术语（名词短语，用于后续向量检索）
2. description: 若节点已给出描述（"已有描述: ..."），直接沿用，不要修改；
   若没有描述，生成一句话说明该节点分析什么（20~40 字）
3. name 字段与输入保持完全一致，不要修改

只输出 JSON，不要有任何解释说明：
{
  "nodes": [
    {
      "name": "节点名称（与输入一致）",
      "keywords": ["关键词1", "关键词2", ...],
      "description": "描述文本"
    }
  ]
}
