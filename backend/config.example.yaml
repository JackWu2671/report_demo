# API 配置模板 — 复制为 config.yaml 并填写真实值
# config.yaml 已在 .gitignore 中，不会提交到仓库

api:
  base_url: "https://<HOST>:<PORT>/rest/nmeatomapigatewayservice/open-api/v1/api-task"
  city: ""
  operator: ""
  province: ""
  user_id: ""
  country: ""
  app_name: "smart-u"
  verify_ssl: false
  timeout: 60
  max_retries: 10
